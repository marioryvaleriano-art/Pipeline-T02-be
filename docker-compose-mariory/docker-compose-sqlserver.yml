version: '3.9'

services:

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest

    container_name: sqlserver-mariory

    environment:
      ACCEPT_EULA: "Y"
      SA_PASSWORD: "TuPasswordFuerte123!"

    ports:
      - "1433:1433"

    restart: always